initialData = {
    "selectedNavItem" : "#{rawJS navItem}"
    , "babiesOnline" : #{rawJS babiesOnline}
    , "currentDate" : new Date().getTime()
}
Elm.fullscreen(Elm.Main, {"initialJSModel" : initialData})
